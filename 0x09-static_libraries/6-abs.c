#include "main.h"
/**
  * _abs - Checks the integer of numbers
  *
  *@i: Input number as an integer
  *
  *Return: - absolute value
  *
  */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (-i);
	}
}
